# Nuttie 项目规则

## 设计系统优先级：P0

统一设计系统是本项目最高优先级，优先级高于任何单端视觉偏好、页面局部优化或临时交互实现。Nuttie 的 Android、iOS、React Native Web 和移动 H5 必须共享同一套品牌角色、设计语言、语义状态、design tokens 和组件行为；平台差异只能改变输入方式、导航承载和密度，不能改变品牌含义、信息架构或状态语义。

权威来源按以下顺序执行：

1. `DESIGN.md`：产品视觉方向、品牌角色、设计语言和跨端不变量。
2. `packages/design-tokens/src/index.ts`：可执行的颜色、排版、间距、尺寸、断点、动效、层级和组件基线。
3. `packages/ui/src`：跨端共享的视觉组件实现。
4. `apps/app`：路由和平台适配，只能组合或扩展上述合同，不能另建一套视觉系统。

任何 UI 改动都必须满足：

- 新颜色、字号、字距、圆角、间距、触控尺寸和断点先进入 tokens，再被页面使用；禁止在页面内创建同义硬编码值。
- 语义状态必须同时有文字或结构表达，颜色只能作为辅助；`quiet`、`growing`、`complete`、`syncing`、`pending`、`conflict` 的含义跨端保持一致。
- 所有交互目标至少 44px；Web 必须有键盘焦点和语义，Native 必须有等价无障碍标签。
- 320px、390px、430px、600px、768px、1024px 和 1440px 宽度，以及深色模式、横屏、长中文和系统字号放大，必须保持不溢出、不遮挡、不改变信息优先级。
- 禁止渐变、发光、装饰性 blob、只用颜色表达状态、伪造医疗结论和把增长标记做成分数/惩罚机制。
- 设计系统变更必须同时更新 `DESIGN.md` 或对应合同文档、tokens、验证器和必要的跨端截图/测试；没有验证的视觉改动不能视为完成。

提交前至少运行 `pnpm test`、`pnpm typecheck` 和 `pnpm build`。无法运行真机或 Docker 时，必须在交付说明中明确列出外部验证缺口，不得将模拟结果写成已完成。
